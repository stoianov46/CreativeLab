import type { Article } from "@/content/types";

/**
 * Journal articles (proposal §22 clusters). Empty until real, researched
 * articles are written — never backdated or filler. Add an entry here and it
 * gets a page at /journal/<slug>, a card on /journal, Article schema and a
 * sitemap entry; then `npm run i18n:sync`.
 */
export const ARTICLES: Article[] = [];
